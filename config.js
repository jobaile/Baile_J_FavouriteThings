const config = {
    user: "root",
    password: "root",
    host: "localhost",
    //port: "3306", //this is my host
    port: "8889", //default port for mamp users
    database: "db_favouritethings"
}

module.exports = config;